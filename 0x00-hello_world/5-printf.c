#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: Always 0 (success)
 * C program that uses the printf function to print in stdout
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
