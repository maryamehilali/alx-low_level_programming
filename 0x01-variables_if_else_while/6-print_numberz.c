#include <stdio.h>

/**
 * main - program to print numbers for 0 to 9
 *
 * Return: Always 0 that means success
 */

int main(void)

{
	int a;

	for (a = 48; a <= 57; a++)
	{
		putchar (a);
	}
	putchar ('\n');

	return (0);
}
